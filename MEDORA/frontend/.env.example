# API base URL of the MEDORA FastAPI backend.
# Leave empty to use Vite dev proxy (/api -> http://localhost:8000).
# For production, set to your backend origin, e.g. https://api.medora.example.com
VITE_API_BASE_URL=
